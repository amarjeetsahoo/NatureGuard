/**
 * NatureGuard — coach View (stub — built in Feature 8)
 */

export async function render(container) {
  container.innerHTML = `<div class="view"><div class="empty-state"><div class="empty-state-icon">🚧</div><h2 class="empty-state-title">coach</h2><p class="empty-state-desc">Coming soon — check the task.md for build order.</p></div></div>`;
}

